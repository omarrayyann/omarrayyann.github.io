export class Project {
    constructor(title, date, image, shortInfo, longInfo) {
      this.title = title;
      this.date = date;
      this.image = image;
      this.shortInfo = shortInfo;
      this.longInfo = longInfo;
    }
  }

  